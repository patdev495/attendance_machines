# Wave 1 Plan: ShiftRule Migration & Logic Refactor

This wave migrates the hardcoded "Xưởng 1" rules into a dynamic `ShiftRule` model and updates the core shift retrieval logic.

## 🎯 Objectives
- **Dynamic Rules**: Move hardcoded logic from code to database.
- **Parity**: Ensure that the same rules currently in `shift_utils.py` are applied via the DB.

## 📝 Tasks

### 1. Database Model Implementation
- [ ] **Modify** [database.py](file:///d:/Workspace/Time_Attendance_Machine/backend/src/database.py) to include `ShiftRule` model.
- [ ] **Run** `init_db()` (via a temporary script or local check) to create the table.

### 2. Data Migration & Seeding
- [ ] **Create** a seed script to populate `ShiftRules` with:
    - **Rule A** (Xưởng 1, D): 20:00 → 08:00 (Next Day), 12h standard, no break, no OT.
    - **Rule B** (Xưởng 1, Default): 08:00 → 20:00 (Same Day), 12h standard, no break, no OT.
    - **Rule C** (Default, D): 20:00 → 05:00 (Next Day), 8h standard, deduct break, has OT.
    - **Rule D** (Default, Default): 08:00 → 17:00 (Same Day), 8h standard, deduct break, has OT.

### 3. Logic Refactoring
- [ ] **Modify** [shift_utils.py](file:///d:/Workspace/Time_Attendance_Machine/backend/src/utils/shift_utils.py) to:
    - Import `ShiftRule` and `SessionLocal`.
    - Query the DB for a matching rule (keyword-based) on each request.
    - Implement a simple thread-local or global cache for the rules to minimize DB lookups if performance becomes an issue.

## 🧪 Verification Plan

- [ ] **Unit Test (Manual)**: Verify `get_shift_rules` returns identical dicts as the original hardcoded functions for all 4 test cases.
- [ ] **API Check**: Ensure attendance summaries for "Xưởng 1" employees are unchanged before and after the migration.
