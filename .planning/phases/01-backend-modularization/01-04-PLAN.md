# Plan: Phase 01-04 - Entry Point Refactor (Wave 4)

## Goal
Transform the monolithic `backend/src/main.py` into a clean, minimal entry point that registers the new modular routers.

---

## Frontmatter
- **Wave**: 4
- **Depends on**: Wave 3
- **Files Modified**: 
  - `backend/src/main.py`
- **Autonomous**: True

---

## Tasks

### 1. Register Modular Routers
Update `main.py` to include the routers created in Wave 3.
<action>
- Modify `backend/src/main.py`.
- Correct all imports from the new `routers`, `services`, and `utils` packages.
- Use `app.include_router(attendance.router)`, `app.include_router(employees.router)`, etc.
</action>
<read_first>
- `backend/src/main.py`
- `backend/src/routers/attendance.py`
- `backend/src/routers/employees.py`
- `backend/src/routers/devices.py`
- `backend/src/routers/sync.py`
</read_first>
<acceptance_criteria>
- `main.py` contains `app.include_router` for all routers.
- The `app` instance is correctly initialized with CORS and Static Files Middleware.
</acceptance_criteria>

### 2. Verify Server Reload
Ensure uvicorn is still running and correctly serving the modularized routes.
<action>
- Check the output of the running `uvicorn` process.
- Verify that the Swagger UI accessible at `/docs` shows the new tags ("Attendance", "Employees", etc.) and all endpoints.
</action>
<read_first>
- `backend/src/main.py`
</read_first>
<acceptance_criteria>
- Uvicorn reloads without errors.
- Swagger UI (`/docs`) displays all expected API endpoints categorized by Tags.
</acceptance_criteria>

### 3. Move Hardcoded Config to config.py
Identify remaining hardcoded values like `XUONG1_DEPT_KEYWORD` and move to `config.py` if not already done.
<action>
- Update `backend/src/config.py` to include the shift keyword.
- Update `utils/shift_utils.py` to import from `config`.
</action>
<read_first>
- `backend/src/config.py`
- `backend/src/utils/shift_utils.py`
</read_first>
<acceptance_criteria>
- `XUONG1_DEPT_KEYWORD` is moved to the `Config` class as an environment variable (or hardcoded default).
- Business logic imports from `config`, not local constants.
</acceptance_criteria>

---

## must_haves
- [ ] Backend is fully modularized with a thin `main.py`.
- [ ] No regressions in API accessibility.
- [ ] Swagger documentation reflects the new architecture.
