# Plan: Phase 01-03 - API Routers (Wave 3)

## Goal
Migrate FastAPI route handlers from `main.py` into modular routers in `backend/src/routers/`.

---

## Frontmatter
- **Wave**: 3
- **Depends on**: Wave 2
- **Files Modified**: 
  - `backend/src/main.py`
  - `backend/src/routers/attendance.py` [NEW]
  - `backend/src/routers/employees.py` [NEW]
  - `backend/src/routers/devices.py` [NEW]
  - `backend/src/routers/sync.py` [NEW]
- **Autonomous**: True

---

## Tasks

### 1. Initialize Routers Directory
Create the `routers/` directory and `__init__.py`.
<action>
Create directory `backend/src/routers/` and empty `backend/src/routers/__init__.py`.
</action>
<read_first>
- `backend/src/main.py`
</read_first>
<acceptance_criteria>
- `backend/src/routers/` exists.
- `backend/src/routers/__init__.py` exists.
</acceptance_criteria>

### 2. Create Attendance Router
Migrate all `/api/attendance/*` and `/api/export-attendance/*` endpoints to `routers/attendance.py`.
<action>
- Create `backend/src/routers/attendance.py`.
- Initialize `APIRouter(prefix="/api/attendance", tags=["Attendance"])` and another for export.
- Copy logic for `@app.get("/api/attendance/date-range")`, `@app.get("/api/attendance")`, `@app.get("/api/attendance/summary")`, and all export endpoints from `main.py`.
- Import necessary services and utils.
</action>
<read_first>
- `backend/src/main.py`
</read_first>
<acceptance_criteria>
- `backend/src/routers/attendance.py` exists with all relevant endpoints.
- Endpoints are using the new `APIRouter` instance.
</acceptance_criteria>

### 3. Create Employee Router
Migrate all `/api/employees/*` endpoints to `routers/employees.py`.
<action>
- Create `backend/src/routers/employees.py`.
- Initialize `APIRouter(prefix="/api/employees", tags=["Employees"])`.
- Copy logic for employee sync status and deletion status from `main.py`.
- Import necessary services and utils.
</action>
<read_first>
- `backend/src/main.py`
</read_first>
<acceptance_criteria>
- `backend/src/routers/employees.py` exists with all relevant endpoints.
</acceptance_criteria>

### 4. Create Device Router
Migrate all `/api/devices/*` and `/api/machines` endpoints to `routers/devices.py`.
<action>
- Create `backend/src/routers/devices.py`.
- Initialize `APIRouter(prefix="/api/devices", tags=["Devices"])`.
- Move machine list and capacity logic from `main.py`.
- Import necessary services and utils.
</action>
<read_first>
- `backend/src/main.py`
</read_first>
<acceptance_criteria>
- `backend/src/routers/devices.py` exists with all relevant endpoints.
</acceptance_criteria>

### 5. Create Sync Router
Migrate the `/api/sync` and other trigger endpoints to `routers/sync.py`.
<action>
- Create `backend/src/routers/sync.py`.
- Move data sync triggers from `main.py`.
- Import necessary services and utils.
</action>
<read_first>
- `backend/src/main.py`
</read_first>
<acceptance_criteria>
- `backend/src/routers/sync.py` exists with all relevant endpoints.
</acceptance_criteria>

---

## must_haves
- [ ] Every endpoint currently in `main.py` is represented in a new router.
- [ ] Routers use individual `APIRouter` instances for better categorization.
