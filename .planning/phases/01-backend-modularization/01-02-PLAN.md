# Plan: Phase 01-02 - Services Extraction (Wave 2)

## Goal
Decouple business logic from route handlers by creating dedicated services in `backend/src/services/`.

---

## Frontmatter
- **Wave**: 2
- **Depends on**: Wave 1
- **Files Modified**: 
  - `backend/src/main.py`
  - `backend/src/services/attendance_service.py` [NEW]
  - `backend/src/services/export_service.py` [NEW]
- **Autonomous**: True

---

## Tasks

### 1. Initialize Services Directory
Create the `services/` directory and `__init__.py`.
<action>
Create directory `backend/src/services/` and empty `backend/src/services/__init__.py`.
</action>
<read_first>
- `backend/src/main.py`
</read_first>
<acceptance_criteria>
- `backend/src/services/` exists.
- `backend/src/services/__init__.py` exists.
</acceptance_criteria>

### 2. Extract Attendance Logic
Create the `AttendanceService` class in `services/attendance_service.py` and move data aggregation logic.
<action>
- Create `backend/src/services/attendance_service.py`.
- Define `AttendanceService` class.
- Move `compute_day_stats` from `main.py` to `services/attendance_service.py` OR import from `utils/stats_utils.py`?
- **Decision**: Import `calculate_attendance_stats` from `utils.stats_utils` and use it in service.
</action>
<read_first>
- `backend/src/main.py`
</read_first>
<acceptance_criteria>
- `backend/src/services/attendance_service.py` contains `def get_attendance_summary`.
</acceptance_criteria>

### 3. Extract Export Logic
Move `export_status` and `run_export_task` from `main.py` to `services/export_service.py`.
<action>
- Create `backend/src/services/export_service.py`.
- Move `export_status` and `export_lock` from `main.py` (lines 24-33).
- Move `run_export_task` from `main.py` (lines 386-610).
- Move `legacy_export_attendance` to this service as well (if needed, or just delete once refactoring complete).
</action>
<read_first>
- `backend/src/main.py`
</read_first>
<acceptance_criteria>
- `backend/src/services/export_service.py` contains `export_status`.
- `backend/src/services/export_service.py` contains `def run_export_task`.
</acceptance_criteria>

### 4. Integration Check (main.py)
Update `main.py` to use new services and verify the export task still starts correctly.
<action>
- Update `main.py` to import `export_status` and `run_export_task` from `.services.export_service`.
- Verify uvicorn server reloads successfully.
- Trigger a mock export via Swagger UI.
</action>
<read_first>
- `backend/src/main.py`
</read_first>
<acceptance_criteria>
- `main.py` contains imports for `export_status` and `run_export_task`.
- No regression in API endpoint `/api/export-attendance/start`.
</acceptance_criteria>

---

## must_haves
- [ ] Export state (lock, status) is isolated from the main app entry point.
- [ ] Background tasks logic is moved to services.
