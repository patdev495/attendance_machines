# Plan: Phase 01-01 - Utils Extraction (Wave 1)

## Goal
Extract stateless utility functions (shift rules and stats calculation) from the monolithic `main.py` into specialized utility modules.

---

## Frontmatter
- **Wave**: 1
- **Depends on**: None
- **Files Modified**: 
  - `backend/src/main.py`
  - `backend/src/utils/shift_utils.py` [NEW]
  - `backend/src/utils/stats_utils.py` [NEW]
- **Autonomous**: True

---

## Tasks

### 1. Initialize Utils Directory
Create the `utils/` directory and `__init__.py`.
<action>
Create directory `backend/src/utils/` and empty `backend/src/utils/__init__.py`.
</action>
<read_first>
- `backend/src/main.py`
</read_first>
<acceptance_criteria>
- `backend/src/utils/` exists.
- `backend/src/utils/__init__.py` exists.
</acceptance_criteria>

### 2. Extract Shift Rules
Move `XUONG1_DEPT_KEYWORD` and `get_shift_rules` from `main.py` to `utils/shift_utils.py`.
<action>
- Create `backend/src/utils/shift_utils.py`.
- Copy `XUONG1_DEPT_KEYWORD` and `get_shift_rules` from `main.py` (lines 47-65).
- Add necessary imports (e.g. `datetime.time`).
</action>
<read_first>
- `backend/src/main.py`
</read_first>
<acceptance_criteria>
- `backend/src/utils/shift_utils.py` contains `XUONG1_DEPT_KEYWORD`.
- `backend/src/utils/shift_utils.py` contains `def get_shift_rules`.
</acceptance_criteria>

### 3. Extract Day Stats Calculation
Move `compute_day_stats` from `main.py` to `utils/stats_utils.py`.
<action>
- Create `backend/src/utils/stats_utils.py`.
- Copy `compute_day_stats` from `main.py` (lines 67-97).
- Import `get_shift_rules` from `.shift_utils`.
- Add necessary imports from `datetime`.
</action>
<read_first>
- `backend/src/main.py`
</read_first>
<acceptance_criteria>
- `backend/src/utils/stats_utils.py` contains `def compute_day_stats`.
- Correct relative import for `get_shift_rules` is present.
</acceptance_criteria>

### 4. Integration Check (main.py)
Temporarily import extracted utils in `main.py` and verify uvicorn continues to reload.
<action>
- Update `main.py` to import `get_shift_rules` and `compute_day_stats` from `.utils.shift_utils` and `.utils.stats_utils`.
- Comment out or delete the old definitions in `main.py`.
</action>
<read_first>
- `backend/src/main.py`
</read_first>
<acceptance_criteria>
- `main.py` contains imports for `get_shift_rules` and `compute_day_stats`.
- Uvicorn server reloads successfully without "name not defined" errors.
</acceptance_criteria>

---

## must_haves
- [ ] Shift logic is isolated from API logic.
- [ ] Stats logic is isolated from API logic.
- [ ] System remains functional with new imports.
