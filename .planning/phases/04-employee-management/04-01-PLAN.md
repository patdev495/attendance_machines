---
id: 04-01
wave: 1
autonomous: true
objective: Migrate machine operations and registry rebuild logic to features/employees/service.py.
files_modified:
  - backend/src/sync_service.py
  - backend/src/features/employees/service.py
  - backend/src/features/daily_summary/service.py
---

# Plan 4-01 — Backend Service Implementation

Goal: Triển khai backend logic cho Employee Management feature trong `features/employees/service.py`.

<tasks>
<task id="4.1.1">
<read_first>
- `backend/src/sync_service.py`
- `backend/src/features/daily_summary/service.py`
- `backend/src/features/employees/service.py`
</read_first>
<action>
Implement `rebuild_registry` in `backend/src/features/employees/service.py`:
1.  **Step 1: Excel Sync data**: Đọc từ `EmployeeMetadata` và upsert vào `EmployeeLocalRegistry` với `source_status='excel_synced'`.
2.  **Step 2: Machine data**: Duyệt qua tất cả các máy, lấy user list, nếu user_id chưa có trong registry hoặc có status `'log_only'`, upsert với `source_status='machine_only'`.
3.  **Step 3: Log data**: Truy vấn `AttendanceLogs` lấy danh sách distinct `employee_id`. Nếu `employee_id` chưa có trong registry, upsert với `source_status='log_only'`.

Implement machine operations:
- `delete_user_from_all_machines(employee_id)`
- `update_user_name_all_machines(employee_id, new_name)`
- `get_biometric_coverage(employee_id)`
- `get_delete_status()`: Trả về trạng thái của `delete_status`.

Lưu ý: Di chuyển các biến trạng thái `delete_status` và `status_lock` vào `features/employees/service.py`.
</action>
<acceptance_criteria>
- `backend/src/features/employees/service.py` chứa hàm `rebuild_registry` xử lý đủ 3 nguồn dữ liệu.
- Hàm `delete_user_from_all_machines` hoạt động ở background (dùng threading/executor).
- `update_user_name_all_machines` cập nhật cả máy và DB.
</acceptance_criteria>
</task>

<task id="4.1.2">
<read_first>
- `backend/src/sync_service.py`
</read_first>
<action>
Cập nhật `backend/src/sync_service.py`:
- Import các hàm đã di chuyển từ `features.employees.service`.
- Xóa code trùng lặp nhưng vẫn giữ export để tránh lỗi import ở các file khác.
</action>
<acceptance_criteria>
- `sync_service.py` có import `from features.employees.service import ...`.
- Không còn lỗi logic lặp lại.
</acceptance_criteria>
</task>
</tasks>

## Verification Plan
- Chạy script test gọi `rebuild_registry` và kiểm tra DB table `EmployeeLocalRegistry`.
- Kiểm tra log connectivity máy khi gọi `get_biometric_coverage`.
