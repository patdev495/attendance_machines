---
id: 04-01
wave: 1
autonomous: true
objective: Migrate machine operations and registry update logic to features/employees/service.py with dual-name and robustness support.
files_modified:
  - backend/src/database.py
  - backend/src/features/employees/service.py
  - backend/src/sync_service.py
---

# Plan 4-01 — Backend Service & Schema Updates

Goal: Triển khai schema mới và logic xử lý nhân viên với tên kép và xóa hardware-only.

<tasks>
<task id="4.1.1">
<read_first>
- `backend/src/database.py`
</read_first>
<action>
Cập nhật `EmployeeLocalRegistry` class trong `backend/src/database.py`:
- Đảm bảo schema đúng với logic unified registry (không thêm cột machine_name).
</action>
<acceptance_criteria>
- `database.py` có cột `machine_name` trong table `EmployeeLocalRegistry`.
</acceptance_criteria>
</task>

<task id="4.1.2">
<read_first>
- `backend/src/features/employees/service.py`
- `backend/src/sync_service.py`
- `backend/src/features/daily_summary/service.py`
</read_first>
<action>
Implement `update_registry` (đổi tên từ `rebuild_registry`) trong `backend/src/features/employees/service.py`:
1.  **Lấy data từ Excel**: Đọc `EmployeeMetadata`, upsert vào registry (`source_status='excel_synced'`).
2.  **Lấy data từ Machines**: Quét toàn bộ máy, lấy `user_id`. Nếu user chưa có trong registry, upsert với `source_status='machine_only'`.
3.  **Lấy data từ Logs**: Quét `AttendanceLogs`, upsert các ID mới với `source_status='log_only'`.
4.  **Persistence**: Lưu tất cả vào DB sau mỗi bước.
</action>
<acceptance_criteria>
- Hàm `update_registry` hoạt động đúng cho cả 3 nguồn.
- Dữ liệu được lưu (commit) vào table `EmployeeLocalRegistry`.
</acceptance_criteria>
</task>

<task id="4.1.3">
<read_first>
- `backend/src/features/employees/service.py`
</read_first>
<action>
Triển khai logic Robust Machine Operations:
- `delete_user_from_hardware(employee_id)`: 
    - CHỈ xóa trên các máy chấm công. KHÔNG xóa record trong DB.
    - Sử dụng `ThreadPoolExecutor` để chạy song song.
    - Thu thập danh sách IPs thành công và IPs thất bại (offline/timeout).
- `update_employee_info(employee_id, db_name)`:
    - Cập nhật `emp_name` trong registry DB.
    - Cập nhật `EmployeeMetadata` nếu có.
    - Đẩy `emp_name` mới xuống tất cả các máy có kết nối (ZKTeco Name).
</action>
<acceptance_criteria>
- Thao tác xóa không làm mất record trong registry DB.
- Trả về báo cáo chi tiết per-machine IP cho user.
</acceptance_criteria>
</task>
</tasks>

## Verification Plan
- Chạy script migration hoặc `init_db()` để update schema.
- Kiểm tra `update_registry` có điền đúng tên từ Excel.
- Test xóa một nhân viên và verify IP offline được báo cáo đúng.
