---
id: 04-03
wave: 3
depends_on: ["04-02"]
autonomous: true
objective: Triển khai giao diện quản lý nhân viên với update support.
files_modified:
  - frontend/src/features/employees/api.js
  - frontend/src/features/employees/index.vue
  - frontend/src/features/employees/components/EmployeesTable.vue
---

# Plan 4-03 — Frontend Implementation

Goal: Xây dựng giao diện Employee Management với bảng nhân viên và nút Update.

<tasks>
<task id="4.3.1">
<read_first>
- `frontend/src/features/employees/api.js`
</read_first>
<action>
Implement các hàm gọi API trong `frontend/src/features/employees/api.js`:
- `getEmployees(filters)`
- `rebuildRegistry()`
- `getRebuildStatus()`
- `deleteEmployee(id)`
- `getDeleteStatus(id)`
- `updateEmployeeName(id, name)`
- `getBiometricCoverage(id)`
</action>
<acceptance_criteria>
- File `api.js` export đầy đủ các hàm cần thiết.
- Sử dụng backend URL từ config (thường là `/api/employees`).
</acceptance_criteria>
</task>

<task id="4.3.2">
<read_first>
- `frontend/src/features/employees/index.vue`
</read_first>
<action>
Xây dựng `EmployeesView` (trong `index.vue`):
- Layout chính bao gồm Filter Bar, Action Buttons (Rebuild Registry), và EmployeesTable.
- Quản lý state cho filters, loading, và pagination.
- Xử lý polling cho rebuild status.
</action>
<acceptance_criteria>
- View hiển thị đúng khi vào route `/employees`.
- Nút "Rebuild Registry" trigger API và hiển thị status (loading/progress).
</acceptance_criteria>
</task>

<task id="4.3.3">
<read_first>
- `frontend/src/features/employees/components/EmployeesTable.vue`
</read_first>
<action>
Xây dựng `EmployeesTable.vue`:
- Các cột: ID, Name, Department, Group, Shift, Source Status (Badge).
- Source Status Badge color:
  - `excel_synced`: Green
  - `machine_only`: Orange
  - `log_only`: Gray
- Actions: View Info, Rename (Edit), Delete, Biometric Coverage.
</action>
<acceptance_criteria>
- Hiển thị badge màu đúng như yêu cầu.
- Các dòng dữ liệu machine_only hiển thị `—` cho các field còn thiếu.
</acceptance_criteria>
</task>
</tasks>

## Verification Plan
- Mở browser test Navigation sang tab Employees.
- Kiểm tra hiển thị table và filter search.
- Click "Rebuild Registry" và kiểm tra network tab.
