---
id: 04-04
wave: 4
depends_on: ["04-03"]
autonomous: true
objective: Di chuyển BiometricCoverageModal, triển khai Edit Modal và hoàn thiện feature.
files_modified:
  - frontend/src/features/employees/components/BiometricCoverageModal.vue
  - frontend/src/features/employees/components/EditEmployeeModal.vue
  - frontend/src/features/employees/index.vue
  - frontend/src/router/index.js
---

# Plan 4-04 — Final Wiring & Modal Implementation

Goal: Hoàn thiện tính năng với Form chỉnh sửa và di chuyển các component cần thiết.

<tasks>
<task id="4.4.1">
<read_first>
- `frontend/src/components/employees/BiometricCoverageModal.vue`
</read_first>
<action>
Di chuyển `BiometricCoverageModal.vue` sang `features/employees/components/`.
Cập nhật API calls để sử dụng `features.employees.api`.
</action>
<acceptance_criteria>
- Modal hoạt động trong feature folder.
</acceptance_criteria>
</task>

<task id="4.4.2">
<read_first>
- `frontend/src/features/employees/components/EmployeesTable.vue`
</read_first>
<action>
Xây dựng `EditEmployeeModal.vue`:
- Form cho phép sửa toàn bộ thông tin: `Employee Name` (DB), `Department`, `Group`, `Shift`, `Start Date`.
- Nút Save gọi `api.updateEmployee`.
- Hiển thị loading trong quá trình đồng bộ xuống máy chấm công.
</action>
<acceptance_criteria>
- Click "Edit" trên table mở được Modal với dữ liệu cũ.
- Save thành công cập nhật được table và đồng bộ tên mới xuống máy.
</acceptance_criteria>
</task>

<task id="4.4.3">
<read_first>
- `frontend/src/router/index.js`
</read_first>
<action>
Final Wiring:
- Đảm bảo route `/employees` trỏ tới `features/employees/index.vue`.
- Verify menu navigation.
</action>
<acceptance_criteria>
- Route hoạt động trơn tru.
</acceptance_criteria>
</task>
<task id="4.4.4">
<read_first>
- `frontend/src/features/employees/components/EmployeesTable.vue`
</read_first>
<action>
Xây dựng `EmployeeInfoModal.vue`:
- Hiển thị toàn bộ trường thông tin từ registry: ID, Name, Department, Group, Shift, Start Date, Source Status, Updated At.
- Nếu trường nào rỗng/null, hiển thị `-`.
- Nút Close để đóng modal.
</action>
<acceptance_criteria>
- Click "View Info" trên table mở được Modal với đầy đủ thông tin.
- Dữ liệu trống được hiển thị là `-`.
</acceptance_criteria>
</task>
</tasks>

## Verification Plan
- Test Edit -> Change Name -> Check if list updates.
- Verify Delete action doesn't remove the row from the table (it should just change the coverage/status or show the user is gone from machines).
