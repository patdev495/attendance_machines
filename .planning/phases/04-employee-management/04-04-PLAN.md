---
id: 04-04
wave: 4
depends_on: ["04-03"]
autonomous: true
objective: Di chuyển BiometricCoverageModal và hoàn thiện feature.
files_modified:
  - frontend/src/features/employees/components/BiometricCoverageModal.vue
  - frontend/src/features/employees/index.vue
  - frontend/src/router/index.js
---

# Plan 4-04 — Final Wiring & Modal Migration

Goal: Hoàn thiện tính năng và di chuyển các component dùng chung vào feature slice.

<tasks>
<task id="4.4.1">
<read_first>
- `frontend/src/components/employees/BiometricCoverageModal.vue` (cũ)
</read_first>
<action>
Di chuyển `BiometricCoverageModal.vue` từ `components/employees/` sang `frontend/src/features/employees/components/`.
Cập nhật API calls bên trong modal để sử dụng `features.employees.api`.
Đảm bảo style và layout không bị vỡ.
</action>
<acceptance_criteria>
- Component mới tồn tại trong feature folder.
- Modal load đúng dữ liệu per-machine.
</acceptance_criteria>
</task>

<task id="4.4.2">
<read_first>
- `frontend/src/features/employees/index.vue`
- `frontend/src/features/employees/components/EmployeesTable.vue`
</read_first>
<action>
Wiring modal vào EmployeesTable:
- Khi click "Biometric Coverage", mở modal với `employee_id` tương ứng.
- Xử lý close modal.
</action>
<acceptance_criteria>
- Click action mở đúng modal cho nhân viên đó.
</acceptance_criteria>
</task>

<task id="4.4.3">
<read_first>
- `frontend/src/router/index.js`
</read_first>
<action>
Đăng ký route `/employees` trỏ tới `features/employees/index.vue` nếu chưa có.
Đảm bảo menu chính trỏ tới đúng route này.
</action>
<acceptance_criteria>
- User có thể truy cập trang Employees từ navigation.
</acceptance_criteria>
</task>
</tasks>

## Verification Plan
- Test end-to-end: Load Employees list -> Filter -> View Biometric Coverage -> Rename -> Delete.
- Kiểm tra console log xem có lỗi import/API nào không.
