---
id: 02-02
wave: 2
autonomous: true
objective: Cài đặt các endpoint cho Log Feature.
files_modified:
  - backend/src/features/logs/router.py
  - backend/src/routers/attendance.py
---

# Plan 2-02 — Backend Router Implementation

Goal: Cài đặt các endpoint cho Log Feature.

<tasks>
<task id="2.2.1">
<read_first>
- `backend/src/routers/attendance.py`
- `backend/src/features/logs/router.py`
</read_first>
<action>
Triển khai các endpoints trong `features/logs/router.py`:
- `GET /` (prefix `/api/logs` đã có): Logic phân trang và filter (employee_id, machine_ip, date range). Copy từ `routers/attendance.py`.
- `GET /date-range`: Copy từ `routers/attendance.py`.
- `POST /sync`: Gọi `BackgroundTasks.add_task(sync_all_machines)`.
- `GET /sync/status`: Trả về `sync_status`.
</action>
<acceptance_criteria>
- Endpoint `GET /api/logs` trả về đúng dữ liệu paginated.
- Endpoint `POST /api/logs/sync` khởi tạo background task.
- Endpoint `GET /api/logs/sync/status` trả về đúng trạng thái (bao gồm cả fail_count).
</acceptance_criteria>
</task>
</tasks>
