---
id: 02-04
wave: 3
autonomous: true
objective: Xây dựng giao diện Log Management theo feature-slice.
files_modified:
  - frontend/src/features/logs/components/LogsFilters.vue
  - frontend/src/features/logs/index.vue
---

# Plan 2-04 — Frontend Feature UI

Goal: Xây dựng giao diện Log Management theo feature-slice.

<tasks>
<task id="2.4.1">
<read_first>
- `frontend/src/features/logs/components/LogsFilters.vue`
</read_first>
<action>
Tạo `frontend/src/features/logs/components/LogsFilters.vue`.
Bố cục: Top bar với các trường Employee ID, Machine IP, Date Picker.
Sử dụng logic của `AttendanceFilters.vue` nhưng cập nhật để emit sự kiện `filter` cho component cha.
</action>
<acceptance_criteria>
- Component hiển thị đúng các filters yêu cầu.
</acceptance_criteria>
</task>

<task id="2.4.2">
<read_first>
- `frontend/src/features/logs/index.vue`
</read_first>
<action>
Hoàn thiện `index.vue` của Log Feature:
- Render `LogsFilters`.
- Render `LogsTable` (tận dụng logic `RawLogsTable.vue`).
- Thêm Watcher trên `syncStore.syncRunning`: Khi trạng thái chuyển từ `True` sang `False`, tự động gọi `loadData()` để refresh bảng log (Theo yêu cầu người dùng).
- Cập nhật thông báo Sync lỗi: Sử dụng `syncStatus.fail_count` để hiển thị trên banner nếu > 0.
</action>
<acceptance_criteria>
- Giao diện Log Feature hoạt động đầy đủ.
- Bảng log tự động refresh sau khi sync xong.
- Banner hiển thị số máy lỗi nếu có.
</acceptance_criteria>
</task>
</tasks>
