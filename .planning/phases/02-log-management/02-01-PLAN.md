---
id: 02-01
wave: 1
autonomous: true
objective: Trích xuất logic sync log từ sync_service.py sang features/logs/service.py.
files_modified:
  - backend/src/sync_service.py
  - backend/src/features/logs/service.py
---

# Plan 2-01 — Backend Service Migration

Goal: Trích xuất logic sync log từ `sync_service.py` sang `features/logs/service.py`.

<tasks>
<task id="2.1.1">
<read_first>
- `backend/src/sync_service.py`
- `backend/src/features/logs/service.py`
</read_first>
<action>
Move `get_machine_list`, `sync_status`, `status_lock`, và `sync_all_machines` từ `sync_service.py` sang `features/logs/service.py`.
Cập nhật `sync_all_machines` để track failure count:
- Thêm `"fail_count": 0` vào `sync_status`.
- Trong loop kết nối máy, nếu gặp `exception`, tăng `sync_status["fail_count"] += 1`.
- Đảm bảo giữ nguyên logic deduplication (existing_keys) và batch commit.
</action>
<acceptance_criteria>
- File `backend/src/features/logs/service.py` chứa mã nguồn đã di chuyển.
- Biến `sync_status` có trường `fail_count`.
- `sync_all_machines` có try-except block tăng `fail_count` khi kết nối lỗi.
</acceptance_criteria>
</task>

<task id="2.1.2">
<read_first>
- `backend/src/sync_service.py`
</read_first>
<action>
Để tránh phá vỡ code cũ ngay lập tức, hãy import ngược lại các hàm đã di chuyển từ `features.logs.service` vào `sync_service.py`.
</action>
<acceptance_criteria>
- `sync_service.py` không còn định nghĩa gốc của các hàm đã move mà dùng `from features.logs.service import ...`.
</acceptance_criteria>
</task>
</tasks>
