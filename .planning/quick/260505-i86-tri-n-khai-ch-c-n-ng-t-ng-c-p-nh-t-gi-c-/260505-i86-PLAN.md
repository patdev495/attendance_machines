---
must_haves:
  - Add sync_time_on_machine function in service.py
  - Add router endpoints in router.py to sync time
---

# PLAN

## Task 1: Add time sync logic and API endpoints
**files:**
- backend/src/features/machines/service.py
- backend/src/features/machines/router.py

**action:**
1. In `service.py`, add `sync_time_on_machine(ip: str)` which connects to the machine using `pyzk`, disables it, calls `set_time(datetime.now())`, and enables it. Handle exceptions and return status.
2. In `service.py`, add `bulk_sync_time_all_machines()` to iterate over all machines and call `sync_time_on_machine`.
3. In `router.py`, add `POST /{ip}/sync-time` endpoint that calls `sync_time_on_machine`.
4. In `router.py`, add `POST /sync-time-all` endpoint that calls `bulk_sync_time_all_machines`.

**verify:**
- The new functions are correctly importing `datetime`.
- The new endpoints are exposed and return correct status.

**done:**
- [ ] Task 1 completed

## PLANNING COMPLETE
