uv run uvicorn backend.src.main:app --host 0.0.0.0 --port 8001 --reload
cd frontend
npm run build
npm run dev

uv run pyinstaller --onedir --name "AttendanceSystem" `
  --collect-all "uvicorn" `
  --collect-all "fastapi" `
  backend/src/main.py

